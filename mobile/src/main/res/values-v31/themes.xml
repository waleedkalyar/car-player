<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CarPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CarPlayer" parent="Base.Theme.CarPlayer" />



    <style name="Theme.MyApp.Splash" parent="Theme.SplashScreen">
        <!-- Background color -->
        <item name="windowSplashScreenBackground">@color/red</item>
        <!-- Logo -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_player</item>
        <!-- Logo scaling -->
        <item name="windowSplashScreenIconBackgroundColor">@null</item>
        <item name="postSplashScreenTheme">@style/Theme.CarPlayer</item>
    </style>


</resources>