<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CarPlayer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CarPlayer" parent="Base.Theme.CarPlayer" >
    </style>


    <!-- Use this fallback for pre-Android 12 -->
        <style name="Theme.MyApp.Splash" parent="Base.Theme.CarPlayer">
        <item name="android:windowBackground">@color/red</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="postSplashScreenTheme">@style/Theme.CarPlayer</item>
    </style>




</resources>